// PASO 1: Generar botones de trimestres
const trimestresUnicos = [...new Set(CONFIGURACION_CURSO.map(b => b.trimestre))];
let trimestreActual = trimestresUnicos.includes("T3") ? "T3" : trimestresUnicos[0]; // T3 por defecto, fallback a primer trimestre
